{
  "name": "engine",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "startProduction": "npm run build && node build/index.js",
    "start": "nodemon"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.11.1",
    "python-shell": "^2.0.1",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-node": "^9.0.0"
  }
}
